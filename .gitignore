# SQLite database — never commit application data.
# Covers both the local run and the container's bind mount.
data/

# Gradle
.gradle/
build/

# Node — the mcp-server module. dist/ is compiled output, rebuilt by `make build`.
node_modules/
mcp-server/dist/

# IDE
.idea/
*.iml
.vscode/

# OS
.DS_Store
