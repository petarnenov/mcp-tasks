# Pinned deliberately — see vault/specs/task-api.md "Why Micronaut 4.10.17 and not 5.x".
# Micronaut 5 has a JDK 25 baseline; this project targets Java 21, so the 4.x line is required.
micronautVersion=4.10.17
sqliteJdbcVersion=3.53.2.1

org.gradle.caching=true
org.gradle.parallel=true
