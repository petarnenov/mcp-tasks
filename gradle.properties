# Micronaut 5 requires a JDK 25 baseline. Java 25 here is therefore not an independent
# preference — the two move together. See vault/specs/micronaut-5-migration.md.
micronautVersion=5.1.1
sqliteJdbcVersion=3.53.2.1

org.gradle.caching=true
org.gradle.parallel=true
